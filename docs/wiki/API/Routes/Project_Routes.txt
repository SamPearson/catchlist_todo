Content-Type: text/x-zim-wiki
Wiki-Format: zim 0.6
Creation-Date: 2026-01-12T17:23:43-06:00

====== Project Routes ======

===== Overview =====

API endpoints for managing projects and their subtasks. Projects are containers for related tasks with GTD-style metadata (win condition, reason, next step).

All endpoints require authentication via JWT Bearer token.


===== Project Endpoints =====

==== List Projects ====

**GET /api/projects**

Returns all projects for the authenticated user.

| Parameter         | Type    | Location | Required | Description                                                   |
|-------------------|---------|----------|----------|---------------------------------------------------------------|
| include_completed | boolean | query    | no       | If true, includes completed/inactive projects. Default false. |

**Response:** Array of project objects


==== Create Project ====

**POST /api/projects**

Creates a new project.

| Field         | Type    | Required | Description                                                      |
|---------------|---------|----------|------------------------------------------------------------------|
| title         | string  | yes      | Project name (max 200 chars)                                     |
| description   | text    | no       | Overview of the project                                          |
| win_condition | text    | no       | Definition of done (required to activate)                        |
| reason        | text    | no       | Why this project matters (required to activate)                  |
| next_step     | string  | no       | The immediate next action (max 200 chars)                        |
| status        | string  | no       | One of: open, waiting, deferred, declined, stale. Default "open" |
| active        | boolean | no       | Default true; false means "not on deck"                          |

**Response:** Created project object (201)


==== Get Project ====

**GET /api/projects/<project_id>**

Returns a specific project by ID.

**Response:** Project object, or 404 if not found


==== Update Project ====

**PUT /api/projects/<project_id>**

Updates a project's basic fields. Does not handle state changes (status, active, completed)—use dedicated endpoints instead.

| Field         | Type   | Description                           |
|---------------|--------|---------------------------------------|
| title         | string | Project name (cannot be empty)        |
| description   | text   | Overview of the project               |
| win_condition | text   | Definition of done                    |
| reason        | text   | Why this project matters              |
| next_step     | string | The immediate next action             |

**Note:** The fields `status`, `active`, `completed`, and `completed_at` cannot be updated via this endpoint. Attempting to include these fields will return a 400 error. Use the dedicated state-change endpoints below.

**Response:** Updated project object, or 404 if not found, or 400 if disallowed fields are included


==== Delete Project ====

**DELETE /api/projects/<project_id>**

Permanently deletes a project and all its subtasks.

**Response:** 204 No Content, or 404 if not found


===== Project State Management =====

==== Complete Project ====

**PATCH /api/projects/<project_id>/complete**

Marks a project as completed.

* Validates that all subtasks have completed=true
* Returns 400 error if any subtasks are incomplete
* Sets completed=true, completed_at=now
* Automatically sets active=false

**Response:** Updated project object, or 400 if incomplete subtasks exist, or 404 if not found


==== Uncomplete Project ====

**PATCH /api/projects/<project_id>/uncomplete**

Marks a project as not completed.

* Clears completed and completed_at
* Does NOT automatically set active=true

**Response:** Updated project object, or 404 if not found


==== Activate Project ====

**PATCH /api/projects/<project_id>/activate**

Activates a project (sets active=true).

* Validates that both win_condition and reason are populated
* Returns 400 error if either field is missing
* Sets active=true on success

**Response:** Updated project object, or 400 if validation fails, or 404 if not found


==== Deactivate Project ====

**PATCH /api/projects/<project_id>/deactivate**

Deactivates a project (sets active=false).

* No validation required
* Sets active=false

**Response:** Updated project object, or 404 if not found


==== Change Project Status ====

**PATCH /api/projects/<project_id>/status**

Changes a project's status field.

**Request Body:**
| Field  | Type   | Required | Description                                              |
|--------|--------|----------|----------------------------------------------------------|
| status | string | yes      | One of: open, waiting, deferred, declined, stale         |

**Response:** Updated project object, or 400 if status is missing or invalid, or 404 if not found


===== Subtask Endpoints =====

==== List Project Tasks ====

**GET /api/projects/<project_id>/tasks**

Returns all subtasks for a project.

| Parameter         | Type    | Location | Required | Description                                       |
|-------------------|---------|----------|----------|---------------------------------------------------|
| include_completed | boolean | query    | no       | If true, includes completed tasks. Default false. |

**Response:** Array of task objects, or 404 if project not found


==== Create Subtask ====

**POST /api/projects/<project_id>/tasks**

Creates a new task as a subtask of this project.

| Field       | Type    | Required | Description                                                      |
|-------------|---------|----------|------------------------------------------------------------------|
| title       | string  | yes      | Task title (max 200 chars)                                       |
| description | text    | no       | Additional details                                               |
| status      | string  | no       | One of: open, waiting, deferred, declined, stale. Default "open" |
| active      | boolean | no       | Default true                                                     |

**Response:** Created task object (201), or 400 if title is missing or validation fails, or 404 if project not found


==== Attach/Detach Tasks ====

To attach an existing task to a project or detach a task from a project, use the Task endpoints:

* **POST /api/tasks/<task_id>/attach/<project_id>** - Attach task to project
* **POST /api/tasks/<task_id>/detach** - Detach task from project (makes it standalone)

See [[Task Routes]] for details.


===== Project Object =====

| Field         | Type     | Description                                        |
|---------------|----------|----------------------------------------------------|
| id            | integer  | Primary key                                        |
| user_id       | integer  | Owner                                              |
| title         | string   | Project name                                       |
| description   | text     | Overview                                           |
| win_condition | text     | Definition of done                                 |
| reason        | text     | Why this project matters                           |
| next_step     | string   | The immediate next action                          |
| completed     | boolean  | Completion status                                  |
| completed_at  | datetime | When completed (ISO format, null if not completed) |
| status        | string   | Current status                                     |
| active        | boolean  | Active flag                                        |
| tags          | array    | Attached tags                                      |
| principles    | array    | Attached principles                                |
| created_at    | datetime | When created                                       |
| updated_at    | datetime | When last modified                                 |